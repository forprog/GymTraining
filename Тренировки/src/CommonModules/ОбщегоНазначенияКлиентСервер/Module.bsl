// @strict-types


#Область ПрограммныйИнтерфейс

Функция ОписаниеТипаСтрока(ДлинаСтроки = Неопределено) Экспорт

	Массив = Новый Массив;//Массив Из Тип
	Массив.Добавить(Тип("Строка"));
	Если ДлинаСтроки <> Неопределено Тогда
		КвалС = Новый КвалификаторыСтроки(ДлинаСтроки);
		ОписаниеТиповС = Новый ОписаниеТипов(Массив, , КвалС);
	Иначе
		ОписаниеТиповС = Новый ОписаниеТипов(Массив);
	КонецЕсли;
	
	Возврат ОписаниеТиповС;
	
КонецФункции

#КонецОбласти

#Область СлужебныйПрограммныйИнтерфейс

// Код процедур и функций

#КонецОбласти

#Область СлужебныеПроцедурыИФункции

// Код процедур и функций

#КонецОбласти
